package com.illiarb.tmdbcliient.core_test.navigation

import com.illiarb.tmdblcient.core.navigation.Router

class TestRouter : Router {

    override fun executeAction(action: Router.Action) {
    }
}