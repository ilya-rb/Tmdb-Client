def projectInfo = [:]
projectInfo.with {
    applicationId = "com.illiarb.tmdbclient"
    minSdkVersion = 21
    targetSdkVersion = 28
    compileSdkVersion = 28
}

def versions = [:]
versions.with {
    // Gradle plugins
    androidGradle = "3.4.0-beta04"
    versionsGradle = "0.20.0"
    jacocoGradle = "0.8.2"
    buildScanGradle = "1.16"
    firebaseGradle = "4.0.1"
    androidJunit5Gradle = "1.3.2.0"

    // Std
    kotlin = "1.3.20"
    kotlin_coroutines = "1.1.1"

    // Support
    supportLibrary = "1.0.0"
    playCore = "1.3.4"
    constraintLayout = "2.0.0-alpha3"
    play_services_version = "16.0.0"

    // Firebase
    firebaseCore = "16.0.6"
    firebaseConfig = "16.1.3"

    // Arch components
    archComponents = "2.0.0-rc01"
    workManager = "1.0.0-alpha11"
    room = "2.1.0-alpha01"
    navigation = "1.0.0-alpha09"

    // Image
    glide = "4.8.0"

    // DI
    dagger = "2.19"
    javax = "1"

    // Network
    retrofit = "2.4.0"
    coroutinesAdapter = "0.9.2"
    okHttpLogger = "3.11.0"

    // Other
    binaryPrefs = "1.0.0"
    timber = "4.7.1"
    lottie = "2.8.0"
    leakCanary = "1.6.3"
}

def testVersions = [:]
testVersions.with {
    junit = "4.12"
    mockito = "2.8.9"
    mockitoKotlin = "2.1.0"
    spek = "2.0.0"
}

def androidTestVersions = [:]
androidTestVersions.with {
    runner = "1.1.0-alpha1"
    espresso = "3.1.0-alpha1"
}

ext.projectInfo = projectInfo
ext.versions = versions
ext.testVersions = testVersions
ext.androidTestVersions = androidTestVersions